# MNIST + MLP — medium experiment
_base_: default.yaml

dataset:
  name: mnist

model:
  arch: mlp
  mlp_hidden: [256, 128]

training:
  epochs: 15
  batch_size: 256
  learning_rate: 0.05
  max_grad_norm: 1.0
  noise_multiplier: 1.0
  delta: 1.0e-5
